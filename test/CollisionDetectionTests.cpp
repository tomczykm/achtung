#include "gtest/gtest.h"
#include "Collisions.h"

using namespace col;

TEST(CircleRectTests, NoCollision) {
    EXPECT_TRUE(true);
}

TEST(CircleRectTests, Intersection) {
    EXPECT_TRUE(true);
}

TEST(CircleRectTests, CircleInside) {
    EXPECT_TRUE(true);
}

TEST(CircleCircleTests, NoCollision) {
    EXPECT_TRUE(true);
}

TEST(CircleCircleTests, Intersection) {
    EXPECT_TRUE(true);
}

TEST(CircleCircleTests, CircleInside) {
    EXPECT_TRUE(true);
}
